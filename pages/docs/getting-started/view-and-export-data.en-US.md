import Callout from "nextra-theme-docs/callout";
import Image from "@/components/Image";

# View And Download Data

## 1.View Data

Open the `Data` panel, find `@official/amazon-knapsack#T492`, and click the `View` button to view the data.

<Image src="/screenshots/view-data.gif" alt="view-data" />

## 2.Export Data

Click the `Export` button to export data, and support common file formats, such as: .json, .csv, .xls, .xml.

<Image src="/screenshots/export-data.gif" alt="export-data" />

<Callout emoji="💡">
The field name of .xml does not support Chinese, so replace it with field ID
</Callout>
